<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar"> <!--Theme.AppCompat.Light.DarkActionBar -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccentRed</item>
        <item name="backgroundcolor">@color/colorPrimary</item>
        <item name="recyclerviewcolor">@color/colorPrimary</item>
        <item name="textcolor">@color/textBlack</item>

        <item name="android:textColorSecondary">@color/black_overlay</item>

        <!-- font for the entire app -->
        <item name="android:fontFamily">@font/paddis_handwritten</item>

        <!-- Status bar stuff. -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppThemeDark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryNight</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkNight</item>
        <item name="colorAccent">@color/colorAccentRed</item>
        <item name="backgroundcolor">@color/colorPrimaryDarkNight</item>
        <item name="recyclerviewcolor">@color/colorPrimaryNight</item>
        <item name="textcolor">@color/textWhite</item>

        <item name="android:textColorSecondary">@color/textWhite</item>

        <!-- font for the entire app -->
        <item name="android:fontFamily">@font/paddis_handwritten</item>

        <!-- Status bar stuff. -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <!--Login Auth theme-->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--<style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />-->

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
